/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package se_project.commands.not_implemented_yet;

import se_project.Solver;
import se_project.Stack;
import se_project.commands.Command;

/**
 *  
 * @author aless
 */
public class CommandFactory {
/*
    private static VariableCommand command;
    private  static CommandFactory instance = null;
    
    private CommandFactory(VariableCommand command) {
        this.command = command;
    }           
     public static CommandFactory getInstance(VariableCommand command) {
        // Crea l'oggetto solo se NON esiste:
        if (instance == null) {
            instance = new CommandFactory(command);
        }
        return instance;
    }

    public static Command createVariablesCommand(String channel, Character variable) {
        if (channel == null || channel.isEmpty()) {
            return null;
        }
        if ("NewVariableCommand".equals(channel)) {
            return new NewVariableCommand(variable,command);
        } else if ("PushVariableCommand".equals(channel)) {
            return new PushVariableCommand(variable,command);
        }
        return null;
    }
*/
}
